% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{import_fgsea}
\alias{import_fgsea}
\title{import_fgsea}
\usage{
import_fgsea(
  x,
  stats,
  geneSets = NULL,
  setType = "UNKNOWN",
  organism = "UNKNOWN",
  keytype = "UNKNOWN",
  exponent = 1,
  scoreType = "std",
  pAdjustMethod = "BH",
  ...
)
}
\arguments{
\item{x}{result table returned by \code{fgsea::fgsea()}}

\item{stats}{named numeric vector of ranked statistics, sorted in
descending order (sorted automatically with a warning if not)}

\item{geneSets}{the \code{pathways} list used as \code{fgsea()} input}

\item{setType, organism, keytype}{metadata stored in the object slots}

\item{exponent, scoreType}{parameters used to recompute missing
\code{rank}/\code{leading_edge}/\code{core_enrichment} columns}

\item{pAdjustMethod}{method used to compute \code{p.adjust} when the
table has no adjusted p-value column}

\item{...}{additional parameters passed to
\code{\link[enrichit]{as_gseaResult}}}
}
\value{
A \code{gseaResult} object
}
\description{
Import fgsea results
}
\details{
Converts the result table returned by \code{fgsea::fgsea()} into a
\code{gseaResult} object.  The ranked statistics vector (\code{stats})
used as \code{fgsea()} input is required, as most GSEA visualizations
consume the ranked list.  Passing the \code{pathways} list used in the
analysis enables exact running-score plots; without it, gene sets are
rebuilt from the leading edge and plots are approximate.
}
